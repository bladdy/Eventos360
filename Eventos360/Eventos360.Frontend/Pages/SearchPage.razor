@page "/search"

<div class="container-fluid my-4">
    <div class="row">

        <!-- FILTROS -->
        <aside class="col-12 col-lg-3 mb-4">
            <div class="card shadow-sm">
                <div class="card-body">

                    <h5 class="fw-bold mb-3">Filtrar resultados</h5>

                    <!-- Categoría -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Categoría</label>
                        <select class="form-select">
                            <option value="">Todas</option>
                            <option>Fotógrafos</option>
                            <option>DJ</option>
                            <option>Buffet</option>
                        </select>
                    </div>

                    <!-- Sector -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Sector</label>
                        <select class="form-select">
                            <option value="">Todos</option>
                            <option>Salones</option>
                            <option>Exteriores</option>
                        </select>
                    </div>

                    <!-- Cercanía -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Ubicación</label>
                        <div class="form-check">
                            <input class="form-check-input" type="checkbox" id="nearby">
                            <label class="form-check-label" for="nearby">
                                Cerca de mí
                            </label>
                        </div>
                    </div>

                    <!-- Tags -->
                    <div class="mb-3">
                        <label class="form-label fw-semibold">Servicios</label>
                        <div class="d-flex flex-wrap gap-2">
                            <span class="badge bg-light text-dark border">Bodas</span>
                            <span class="badge bg-light text-dark border">XV Años</span>
                            <span class="badge bg-light text-dark border">Corporativo</span>
                        </div>
                    </div>

                    <button class="btn btn-primary w-100">
                        Aplicar filtros
                    </button>

                </div>
            </div>
        </aside>

        <!-- RESULTADOS -->
        <section class="col-12 col-lg-9">

            <!-- Header resultados -->
            <div class="d-flex justify-content-between align-items-center mb-3">
                <h4 class="fw-bold mb-0">
                    Proveedores encontrados
                </h4>

                <select class="form-select w-auto">
                    <option>Relevancia</option>
                    <option>Mejor calificados</option>
                    <option>Más cercanos</option>
                </select>
            </div>

            <!-- LISTADO -->
            <div class="row g-4">

                @for (int i = 0; i < 6; i++)
                {
                    <div class="col-12 col-md-6 col-xl-4">
                        <div class="card h-100 shadow-sm">
                            <img src="/imagenes/categorias/fotografia.jpg"
                                 class="card-img-top"
                                 alt="Proveedor">

                            <div class="card-body">
                                <h5 class="card-title fw-bold">
                                    Nombre del proveedor
                                </h5>

                                <p class="card-text text-muted small mb-2">
                                    Fotografía profesional para eventos
                                </p>

                                <div class="d-flex justify-content-between align-items-center">
                                    <span class="badge bg-success">
                                        ★ 4.8
                                    </span>

                                    <a href="#" class="btn btn-outline-primary btn-sm">
                                        Ver perfil
                                    </a>
                                </div>
                            </div>
                        </div>
                    </div>
                }

            </div>

            <!-- PAGINACIÓN -->
            <nav class="mt-4">
                <ul class="pagination justify-content-center">
                    <li class="page-item disabled">
                        <span class="page-link">Anterior</span>
                    </li>
                    <li class="page-item active">
                        <span class="page-link">1</span>
                    </li>
                    <li class="page-item">
                        <a class="page-link" href="#">2</a>
                    </li>
                    <li class="page-item">
                        <a class="page-link" href="#">Siguiente</a>
                    </li>
                </ul>
            </nav>

        </section>

    </div>
</div>



